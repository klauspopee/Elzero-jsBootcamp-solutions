let needle = "JS";
let haystack = ["PHP", "JS", "Python"];

// Write 3 Solutions
console.log(haystack.includes(needle));
console.log(haystack.indexOf(needle));//if its not -1 it means that the items belongs to the arrry
console.log(haystack.lastIndexOf(needle));//if its not -1 it means that the items belongs to the arrry